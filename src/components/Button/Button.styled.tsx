import styled from 'styled-components/native';

export const ButtonContainer = styled.TouchableOpacity`
	${() => ({ width: '100%', backgroundColor: 'white', alignItems: 'center', padding: 8, borderRadius: 8 })}
`;
